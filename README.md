# 夜班样本交接台

一个真正以 PostgreSQL 为裁决者的样本交接台。FastAPI/SQLAlchemy 保存批次、容器、位置、一次性交接和不可变责任链；React/Vite 页面提供待办、六位短码接收、异常处置及详情。浏览器不会推测写入成功，也不会用本机时间决定交接是否过期。

## 启动

需要 Docker 及 Docker Compose v2。

```bash
cp .env.example .env
# 修改 .env 中的 POSTGRES_PASSWORD、DATABASE_URL 密码和 HANDOFF_SIGNING_KEY
docker compose up --build -d
docker compose ps
```

打开 <http://localhost:4173>；API 文档位于 <http://localhost:8000/docs>。三个服务均有健康检查，且 API、前端以非 root 用户运行。API 会等待数据库可连接，然后执行 `alembic upgrade head`；迁移失败时容器以代码 71 退出并输出明确恢复提示，而不会带着旧 schema 接收写入。

`DATABASE_URL` 中的密码必须与 `POSTGRES_PASSWORD` 相同。例如把密码改为 `a-long-password` 时，同时使用：

```dotenv
POSTGRES_PASSWORD=a-long-password
DATABASE_URL=postgresql+psycopg://handoff:a-long-password@db:5432/handoff
```

仓库不会提交 `.env`、本地密钥、数据库目录或 Docker 生成物。

## 数据库迁移与重置

API 正常启动会自动迁移。手动查看及执行迁移：

```bash
docker compose run --rm --entrypoint alembic api current
docker compose run --rm --entrypoint alembic api upgrade head
```

若迁移失败，先运行 `docker compose logs api db`，根据 Alembic revision/SQLSTATE 修复配置或 schema，再重启 `docker compose up -d api`。不要绕过迁移启动 Uvicorn。

完全重置开发数据（会删除命名数据库卷，不能恢复）：

```bash
docker compose down --volumes
docker compose up --build -d
```

生产备份应在迁移前使用 `pg_dump`；恢复时先恢复同版本数据，再运行 `alembic upgrade head`。

## 业务与并发语义

- 批次记录温区和最长离柜分钟数。位置明确标记是否为冷藏；从冷藏发起到非冷藏位置的交接即开始计时，因此接收人迟到也计入暴露。撤销视为返回来源并结算本次时长；回到冷藏时累计，达到上限（包括恰好等于边界）即拒绝接收并形成异常。
- 六位码由密码学随机源产生，只保存以 `HANDOFF_SIGNING_KEY` 计算的 HMAC-SHA256 摘要。明码只在发起/重开响应展示一次；已经用过的摘要不会重新分配。
- 确认在一个事务中以 `SELECT … FOR UPDATE` 依次锁定交接、容器和批次，再用数据库可见的当前服务端时间校验状态、截止时间、来源位置和暴露上限。只有校验全部通过，才同时移动容器并追加责任链。
- 夜班接收人发现封签破损、标签不符或包装污染时，可凭仍有效的六位码直接拒收（`POST /api/handoffs/reject`，原因限 `seal_broken`、`label_mismatch`、`package_contaminated`、`other`，备注可空）。拒收与确认共用同一事务裁决：同样依次锁定交接、容器、批次并校验待接收、未过期、仍位于来源；通过后交接置为 `anomaly`/`RECEIVER_REJECTED`、批次置为 `review`，容器不移动。若交接从冷藏指向非冷藏，按退回来源结算本次离柜时长并清除 `out_since`，且只追加一次 `handoff_rejected` 责任链事件。重复提交同一拒收返回首次结果并带 `replayed=true`。
- 日常盘点发现容器损坏且样本已转装时，在批次详情对 `active` 容器执行“转装替换”（`POST /api/containers/{id}/replace`，提交新标签、操作人、原因和可空备注）。服务在同一事务以 `SELECT … FOR UPDATE` 依次锁定原容器与批次，校验批次仍可流转且该容器没有待接收交接，再检查新标签在批次内不重名；任一校验失败整体回滚。通过后在原位置创建新容器并继承累计离柜秒数与 `out_since`（计时连续，不重置暴露预算），随后把原容器置为 `replaced`，记录替代容器、替换人、时间与原因，并追加一条 `container_replaced` 责任链事件（事件指向原容器）。原容器已替换返回 `CONTAINER_ALREADY_REPLACED`，存在待接收交接返回 `HANDOFF_ALREADY_PENDING`，新标签重名返回 `CONTAINER_LABEL_EXISTS`。封存容器不能再发起交接（返回 `CONTAINER_ALREADY_REPLACED`），历史交接仍指向原容器且旧接口可正常读取；迁移把全部旧容器置为 `active`。
- 两人同时提交一个有效码时，第二个事务在行锁后看到 `received`，返回与首次相同的接收事实并带 `replayed=true`；不会二次移动或追加事件。两次拒收同理，重复拒收只返回首次拒收事实。确认与拒收并发时仅先获得交接行锁的操作生效：确认胜出后拒收返回 `HANDOFF_ALREADY_RECEIVED`，拒收胜出后确认返回 `HANDOFF_REJECTED`。SQLite 只用于快速单测，生产并发保证来自 PostgreSQL 行锁。
- 样本存储与处理期间的人工测温在批次详情补录：对 `active` 容器提交摄氏温度、测量人、测量时间与可空备注到 `POST /api/containers/{id}/temperature-observations`。批次温区以摄氏度数值上下限（`temp_min_c`/`temp_max_c`）裁决，边界值（恰好等于上/下限）判为 `normal`，严格越界判为 `out_of_range`；原始读数、判定、测量人与测量时间存入 `temperature_observations`，同一事务只追加一条 `temperature_recorded` 时间线事件（事件与观测一一对应，数据库唯一约束保证）。越界时仅把批次置为 `review`，容器位置、`out_since`、累计离柜秒数和交接记录都不变。已封存容器返回 `CONTAINER_ALREADY_REPLACED`；测量时间晚于服务端当前时间或早于批次创建时间返回 `INVALID_OBSERVED_AT`。任一校验失败整体回滚，不留下观测或事件，前端表单保留全部输入。批次详情按测量时间倒序展示温度、判定、测量人与时间。
- 批次温区从旧版自由文本迁移为摄氏度上下限：新建批次可直接给 `temp_min_c`/`temp_max_c`（或继续传 `2–8°C` 这类可解析文本，由后端解析）；迁移 0004 逐行解析现有温区文本并回填上下限，无法识别时在任何 DDL 之前明确报错中止，修正文本后重跑即可，绝不猜测范围。批次响应在保留 `temperature_zone` 的同时新增上下限字段，旧批次读取与交接请求保持兼容。
- 夜班交接前在主导航“位置盘点”核对冷藏点实物：选择冷藏位置后逐行扫描容器标签并提交（`POST /api/locations/{location_id}/inventory-checks`，载荷为 `checked_by` 与非空、去重、去首尾空白的 `labels`）。服务端在同一事务先 `SELECT … FOR UPDATE` 锁定位置，再锁定账面记录在该位置的全部 `active` 容器形成快照，随后以标签集合比对：在本处记录命中为已匹配（matched），账面在本处但未扫到为账面缺失（missing），标签属于其他位置的可流转容器为错放到此处（misplaced），系统中无对应可流转容器标签为未知标签（unknown）。一次扫描的标签只核销一个账面容器：跨批次允许的同标签重名若有两个容器同柜，只扫一次时另一容器仍计入账面缺失。盘点记录（`location_inventory_checks` 及 `location_inventory_check_items`）不可变，保存提交人、服务端时间、每柜严格递增的盘点序号、四个分类计数与逐行明细；最近记录按序号排序，即使服务端时间固定（多个盘点共享同一 `created_at`）也能正确区分先后，且不可变记录重放不会混入后来的盘点。错放行携带系统记录位置编码与名称，供人员按原交接流程纠正。盘点只形成核对证据：不移动容器、不改批次处置、不写交接/测温记录，也不产生责任链事件。重复（忽略大小写）或空白标签返回 `INVALID_INVENTORY_LABELS`，盘点人为空白（仅空格）返回 `INVALID_INVENTORY_CHECKED_BY`，二者均不开事务、不落库，且服务端保存前去首尾空白；位置不存在返回 `LOCATION_NOT_FOUND`；非冷藏位置返回 `LOCATION_NOT_COLD_STORAGE`。已封存容器退出可流转快照：扫到旧标签按未知标签处理。提交响应含本次四类分组明细与该位置本次之前的最近记录（`GET /api/locations/{id}/inventory-checks`）；前端逐行校验原始扫描：任何位置（标签之间、开头或末尾）出现空白行都会在请求前提示“标签清单无效（第 N 行为空白标签）”并保留原文、不发请求、不生成盘点记录——每个标签必须恰好占一行，扫描枪多余的回车需要先删除；盘点人只含空格时同样在请求前拒绝并保留全部输入；断网或提交失败时标签与盘点人保留在表单中，成功后清空已扫标签但保留盘点人以便连续盘点。
- 确认、撤销、拒收和超时相遇时，先获得交接行锁的事务决定结果；刷新后位置、批次处置与时间线来自同一提交。发起人只能在服务端尚未判定过期时撤销。
- 夜班发起交接后若目标处理台临时停用，发起人可在待办详情对仍有效的待接收记录执行“改派目标”（`POST /api/handoffs/{id}/reroute`，提交操作人、新目标位置编码与原因）。服务在同一事务以 `SELECT … FOR UPDATE` 依次锁定交接、容器和批次，校验发起人身份、交接仍待接收且未过期、容器仍在来源位置，再确认新旧目标不同、新目标不等于来源且冷藏属性与当前目标一致；通过后仅更新目标位置，记录可空的原目标位置、改派人、改派时间与原因，并只追加一条 `handoff_rerouted` 责任链事件。六位码与截止时间保持不变，接收人继续使用原码在新目标完成接收；再次改派保留首次记录的原目标位置。改派与确认、拒收或撤销并发时由同一交接行锁裁决：确认胜出后改派返回 `HANDOFF_ALREADY_RECEIVED`，撤销胜出返回 `HANDOFF_CANCELLED`，拒收胜出返回 `HANDOFF_REJECTED`，改派先完成则另一操作按新目标继续裁决。新位置不存在返回 `LOCATION_NOT_FOUND`，等于来源返回 `SAME_LOCATION`，与当前目标相同返回 `REROUTE_TARGET_UNCHANGED`，冷藏属性不符返回 `REROUTE_ENVIRONMENT_MISMATCH`；任一失败整体回滚，不留下改派字段或事件。迁移 0007 为交接记录补充四个可空列，旧记录读取与创建、确认、拒收、撤销的请求响应保持兼容。
- 到期的 pending 记录在读取时以有效状态 `anomaly` 展示为红色；确认时或重开时会持久化异常。未解决异常（包括尚未物化的过期记录）阻止整个批次发起新流转。
- 重开先把原异常解析为 `REOPENED`，保留旧记录和责任链并建立 `successor_id`，再生成新码。也可记录隔离、复核或放行决定；只有放行或重开会恢复 active。
- 网络中断时前端保留接收码与接收人，且不做乐观位置变更。API 错误统一返回 `code/message/retryable/trace_id/details`；数据库中断返回可重试的 503，事务死锁/序列化冲突返回可重试的 409。

## 验收操作

1. 在“批次档案”登记批次，输入摄氏温区上下限（如 2–8°C）、离柜上限、容器和初始位置。
2. 回到“交接待办”发起交接，记下只显示一次的六位码；待办显示根据响应内 `server_time` 建立的单调倒计时。
3. 在“短码接收”输入代码和接收人；成功后刷新批次详情，当前位置和 `handoff_received` 责任链同时出现。
4. 再提交同一码，应显示早前接收结果；批次时间线仍只有一次接收事件。
5. 在“短码接收”切换到“拒绝接收”，选择原因并填写备注后提交：容器仍留在来源位置，批次进入 `review`，时间线仅追加一条 `handoff_rejected`；再提交同一拒收码返回首次结果且 `replayed=true`。详情抽屉展示拒收人、原因与备注，可经隔离/复核/放行或“重开交接”恢复，重开会生成新的一次性码。
6. 发起有效期 1 分钟的交接，等待到期：列表变红，接收返回 `HANDOFF_EXPIRED`。重开后新旧记录通过 successor 关联，旧码不再有效。
7. 盘点发现容器损坏且样本已转装：打开批次详情，在 `active` 容器上点“转装替换”，填写新标签、操作人、原因并提交。原容器显示“已封存”且不再有操作按钮，新容器在同一位置显示“可流转”，离柜计时与 `out_since` 连续；时间线只增加一条 `container_replaced`，历史交接仍可打开且仍指向原容器。待接收交接存在时提交得到 `HANDOFF_ALREADY_PENDING`，标签重名得到 `CONTAINER_LABEL_EXISTS`，失败后页面保留全部输入。
8. 目标处理台临时停用：打开待接收交接的待办详情，在“改派目标”选择同类环境的另一位置（下拉只列出冷藏属性一致、且非来源非当前目标的位置），填写改派人与原因后提交。目标位置与时间线立即刷新，详情记录原目标位置、改派人、时间与原因，原六位码与倒计时不重建；到“短码接收”输入原码即在新目标完成接收，容器只移动到新目标，时间线恰有一条 `handoff_rerouted` 和一条 `handoff_received`。提交失败或断网时改派人、原因与所选位置保留在表单中。
9. 在浏览器开发工具中切换 Offline，提交接收或拒收：全部输入（动作、代码、人员、原因、备注）不会清空，页面不会移动容器；恢复联网后原输入可直接重试。
10. 并发验收可将 `CODE` 和接收人替换后同时执行；两个响应都为接收事实，其中一个 `replayed` 为 true。确认与拒收并发则一方得到事实、另一方得到 `HANDOFF_ALREADY_RECEIVED` 或 `HANDOFF_REJECTED`：

   ```bash
   for who in receiver-a receiver-b; do
     curl -sS -X POST http://localhost:8000/api/handoffs/confirm \
       -H 'Content-Type: application/json' \
       -d "{\"code\":\"$CODE\",\"received_by\":\"$who\"}" &
   done
   wait
   ```

   拒收请求体为 `{"code":"$CODE","rejected_by":"night-lead","reason":"seal_broken","note":"…"}`。

11. 用 `docker compose stop db` 后请求健康检查和写 API，确认得到含追踪号的 503；`docker compose start db` 后可重试原输入。
12. 打开批次详情，在 `active` 容器上点“补录人工测温”，填写 5°C、测量人、测量时间后提交：立即看到“正常”判定，批次仍为 active，时间线只多一条 `temperature_recorded`。再对同一容器补录 9.5°C（对 2–8°C 温区）：判定为“越界”，批次进入 `review`，容器位置与离柜计时不变，时间线同样只有一条对应事件；批次详情按测量时间倒序列出温度、判定、测量人与时间。对已封存容器提交得到 `CONTAINER_ALREADY_REPLACED`；把测量时间改成未来或批次创建之前得到 `INVALID_OBSERVED_AT`，失败后表单温度、人员、时间、备注全部保留。
13. 打开“位置盘点”，选择冷藏冰箱，逐行录入现场扫到的容器标签与盘点人后提交：看到已匹配、账面缺失、错放到此处、未知标签四组计数与明细；错放行显示“系统记录位置”，但批次详情中容器位置、批次处置和责任链均无变化，时间线没有盘点事件。对非冷藏位置无法提交（页面只列冷藏位置，接口返回 `LOCATION_NOT_COLD_STORAGE`）；提交重复或空白标签得到 `INVALID_INVENTORY_LABELS` 且数据库中没有任何盘点记录。断网时标签与盘点人保留，恢复后可原样重试；成功后展示本次结果与该位置的最近盘点记录。

## 测试

```bash
make test                 # 容器内运行 pytest、Vitest 和前端生产构建
docker compose up -d      # 端到端测试前先启动完整栈
cd frontend
npm install
npx playwright install chromium
npm run e2e               # 浏览器创建记录，再由真实 API/数据库读回
```

后端测试覆盖状态转换、撤销权限、错误结构、幂等重放（确认与拒收）、双线程同码竞争、确认/拒收互斥竞争、拒收状态边界（过期、撤销、未知码、非法原因、冷柜离柜结算）、超时/重开历史及 59/60 秒暴露边界。改派覆盖原码在新目标完成接收且时间线恰有一条改派与一条接收事件、二次改派保留首次原目标、非发起人 403、未知位置/等于来源/目标不变/冷藏属性不符的明确错误且不留字段或事件、已确认/已撤销/已拒收/已过期的对应业务冲突、容器不在来源的 LOCATION_MISMATCH、改派与确认/撤销/拒收双线程竞争由行锁裁决的两种确定结果，以及 0007 迁移升级后旧记录可读与降级。转装替换另测离柜计时跨容器连续、待接收交接/重名标签/非 active 批次的冲突回滚、二次转装替代链、历史交接仍指向原容器以及双线程替换仅一个赢家。人工测温覆盖上下限等值与中间值判定为 normal、越界置 review 且位置/离柜/交接不变、事件写入失败时观测与处置原子回滚、封存容器返回 `CONTAINER_ALREADY_REPLACED`、未来时间与早于批次创建时间返回 `INVALID_OBSERVED_AT` 且不留部分结果、测量时间倒序与每观测唯一事件、温区文本解析（含无法识别明确失败）以及 0004 迁移对旧 `2–8°C` 数据的回填与无法识别时中止。位置盘点覆盖全匹配（一份头记录加逐行明细、提交人与服务端时间）、混合差异的四类分类（错放行携带系统记录位置、账面缺失带账面标签）、盘点不移动容器/不改处置/不写责任链、重复或空白标签返回 `INVALID_INVENTORY_LABELS` 且不落库、位置不存在与非冷藏拒绝、明细写入失败时头记录同事务回滚且可干净重试、不可变记录与最近记录顺序、已封存容器退出快照、待接收交接期间容器仍在原柜快照内、同柜同标签两个容器只扫一次时另一容器计入账面缺失、固定服务端时间连续提交时第二次带回首次记录（含第三次链式回放与不可变重放）、盘点人仅空格被拒且不落库，以及 0006 迁移的升降级（旧盘点按位置回填为无空隙序号）。前端 Vitest 覆盖基于服务器快照和单调时钟的倒计时，以及领域错误、断网反馈映射、拒收表单保留；改派另验证只列同类环境位置、提交载荷、失败/断网保留改派人原因与所选位置、成功后目标立即刷新且原码与倒计时不重建、非待接收记录不显示改派入口，并验证新容器可发起交接、封存容器不可操作、替换失败保留输入且成功后时间线仅一条替换记录；测温另验证提交载荷（UTC 测量时间）、判定与测量信息倒序展示、失败保留全部输入；盘点另验证标签解析/去重、只列冷藏位置、提交载荷、盘点人空白在请求前拒绝并保留输入、结果四组分组与错放位置、成功后清空标签但保留盘点人、失败/断网保留全部输入以及最近记录加载。Playwright 测试贯通页面、API 与 PostgreSQL，并验证拒收后位置不变、批次进入复核、时间线仅一条拒收事件且重开可生成新码；转装流程验证原容器封存、新容器继续交接及唯一替换事件；测温流程贯通正常测温保持 active 与越界进入复核，且各自只产生一条 `temperature_recorded` 事件；盘点流程贯通混合差异四分类，并确认只生成一份快照（一头记录、逐行明细），容器位置、批次处置与责任链完全未变化。改派流程从浏览器创建交接、详情改派到原码接收，确认容器只移动到新目标且时间线恰有一条改派和一条接收记录。

## 密钥轮换提示

当前版本的未完成交接依赖单个 `HANDOFF_SIGNING_KEY`。轮换前应先撤销或处理全部 pending 交接，再替换环境变量并重启 API；否则旧码无法查找。这是有意的安全失败模式，历史记录和责任链本身不受影响。
